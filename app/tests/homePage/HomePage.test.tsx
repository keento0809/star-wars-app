import { FilmsQuery } from "@/app/graphql/generated";
import { HomePage } from "@/app/HomePage";

// describe("homePage", () => {
//   const mockData: FilmsQuery = { allFilms: [] };

//   it("should render data when the page is loaded", () => {
//     render(<HomePage data={mockData} />);
//   });
// });
